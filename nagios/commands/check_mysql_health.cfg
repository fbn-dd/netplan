define command{
        command_name    check_mysql_health
        command_line    /usr/lib/nagios/plugins/check_mysql_health --hostname $HOSTADDRESS$ --username $USER3$ --password $USER4$ --method dbi --mode '$ARG1$'
	}
